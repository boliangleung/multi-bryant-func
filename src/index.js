import * as filters from './filter'
import * as message from './message'

// const fileName = fs.readdirSync(path.resolve(__dirname))
// console.log(fileName)
export { filters, message }
export default {
  lbl: 666
}
